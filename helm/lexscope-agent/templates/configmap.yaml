apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "lexscope-agent.fullname" . }}-config
  labels:
    {{- include "lexscope-agent.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
