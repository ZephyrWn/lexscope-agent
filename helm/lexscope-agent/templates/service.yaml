apiVersion: v1
kind: Service
metadata:
  name: {{ include "lexscope-agent.fullname" . }}
  labels:
    {{- include "lexscope-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lexscope-agent.selectorLabels" . | nindent 4 }}
